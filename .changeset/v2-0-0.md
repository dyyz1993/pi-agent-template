---
"@dyyz1993/rpc-core": major
"@dyyz1993/create-agent": major
---

## v2.0.0 — Breaking Changes

### rpc-core

- Enable strict TypeScript mode (noUncheckedIndexedAccess, noImplicitReturns)
- Stricter type safety across all APIs

### create-agent

- Add eslint-plugin-react-hooks, react-refresh, jsx-a11y to templates
- Add Prettier code formatting with lint-staged integration
- Promote ErrorBoundary to shared module (all templates)
- Add env.d.ts for type-safe environment variables
- Unify lockfile to pnpm (remove bun.lock)
- Unify Node.js to v22 across all workflows
- Add CI pnpm cache for faster builds
- Add shared module tests (logger, http-routes)
- Rewrite project rules to be concise and standards-compliant
