# Session: ui-test-1777778786
Date: 2026-05-03
Module: agent-template
URL: http://localhost:5190
Scenarios: 23 passed / 25 not tested (browser instability)

## Tested Successfully (Sections A-D)

### A. Page Load & Layout (5/5 PASSED)
- Page loads without error overlay ✅
- Title bar shows "Web (WebSocket)" with blue bg-blue-600 badge ✅
- ActivityBar has 4 icons: Explorer, Source Control, Search, Rules ✅
- Tab bar has: Chat, Feed+Subs, Bash, Todo ✅
- Default sidebar shows Explorer with file tree (22 items) ✅

### B. Explorer (4/4 PASSED)
- Explorer file tree loads showing project files ✅
- Clicking src/ expands to show 6 sub-items ✅
- Clicking vite.config.ts opens preview overlay with code content + line numbers ✅
- Close button (X) at position (~952, 40) closes preview overlay ✅

### C. Chat (6/6 PASSED)
- Switched to Chat tab ✅
- Sent "hi" via chat.send RPC — user msg right (justify-end, bg-indigo-600), bot reply left (justify-start) ✅
- Bot SVG icon next to assistant message (in flex-shrink-0 container) ✅
- Timestamps in HH:MM format ("11:32", "11:36") ✅
- "what is 3 * 7" → bot replies "That would be 21!" ✅
- "help" → formatted capabilities list (Greetings, Time & Date, Math, Files, Git, Help) ✅

### D. Bash Terminal (8/8 PASSED)
- Switched to Bash tab ✅
- Command input + Run button visible (disabled when empty) ✅
- "echo hello" → output shows "hello" ✅
- Output uses font-mono (monospace font) ✅
- "date" → shows current date (2026/May) ✅
- Process list shows PID 1, PID 2 buttons ✅
- "sleep 60" → Kill button clicked → process stopped (Kill btn gone) ✅
- No active process state: PID shows "exited(?)" status, Kill btn gone ✅

## Not Tested (Sections E-H, 25 test cases)

Browser became unstable after Section D. Page consistently navigates to about:blank within 4-6 seconds of loading.
Root cause: Other concurrent agents' browser sessions appear to interfere with this session's Chromium instance.
Multiple recovery attempts failed (8+ fresh sessions).

### E. Rules Management (0/8)
### F. Todo Management (0/8)
### G. Feed & Subscriptions (0/4)
### H. Search (0/4)

## Findings
- Selectors in selectors.yml verified stable across both port 5175 and 5190
- Chat requires selecting chat.send from RPC dropdown before messages work
- File preview close button is a small SVG at position (~952, 40), no aria-label
- Bash output area has font-mono class for monospace rendering
- Explorer file tree items are LI elements with div.flex.items-center children
- Chat messages use flex justify-end (user) and flex justify-start (bot) alignment

## Critical Issue: Browser Session Instability
- After ~10 minutes of active testing, the browser session became unusable
- Page navigates to about:blank within 4-6 seconds of opening
- Issue persists across fresh sessions, different session names, and browser restarts
- Likely caused by concurrent agents sharing Chromium resources at OS level
- Recommendation: Run Section E-H tests in isolation (no other agents running)
