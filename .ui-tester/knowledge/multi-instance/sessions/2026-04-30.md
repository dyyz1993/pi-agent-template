# Session: ui-test-1777479318
Date: 2026-04-30
Module: multi-instance
Scenarios: 2 passed / 1 partial (chat send failed) / servers crashed

## Instance Map (This Run)
| Instance | Vite URL   | Backend Port | Title              | Result           |
|----------|------------|-------------|--------------------|------------------|
| 1        | :5175      | 3101        | Verify-1           | PASS (full)      |
| 2        | :5174      | 3102        | Verify-2           | PARTIAL (chat error) |
| 3        | :5173      | 3100        | Pi Agent Chat (different app!) | PASS (full) |

## Tested
- Instance 1 (localhost:5175): WS auto-connected, chat send+response works. PASS
- Instance 2 (localhost:5174): WS auto-connected, app loaded, but chat.send RPC fails with "WebSocket connection failed". Message never sent. PARTIAL
- Instance 3 (localhost:5173): Different app entirely (Pi Agent Chat, not pi-agent-template). WS connected, chat send+response works. PASS
- Isolation check: NOT PERFORMED - instances 1 & 2 Vite servers crashed mid-test before revisit

## Key Findings
1. Instance 3 (port 5173/backend 3100) is NOT the same app template — it's "Pi Agent Chat" from /Users/xuyingzhou/Project/temporary/pi-agent-chat
2. Instance 2 had persistent "Chat error: WebSocket connection failed" — RPC chat.send failed despite WS main connection working
3. Instances 1 & 2 Vite servers crashed during the test (ports 5175, 5174 went offline)
4. Instance 3 (the Pi Agent Chat) remained stable throughout
5. WS auto-connect through Vite proxy works without query params (confirmed for all 3)
6. Chat messages NOT visible in snapshot -i — must use `get text body`

## WS Auto-Connect Pattern
- Vite proxy handles /ws → backend automatically
- No ?ws= or ?token= query params needed
- App shows "Web (WebSocket)" in title bar when connected
- Spinner "Connecting to RPC server..." shows until WS connects

## Instance 3 UI Differences
- Full Chinese-language interface
- Title: "Pi Agent Chat"
- Input: "输入消息、@file、@agent，或粘贴图片与文本..."
- Send button: "发送"
- Rich sidebar with conversation list, Git, Files, Status panels
- ActivityBar icons are different from instances 1 & 2

## Updated
- selectors.yml: Needs update for instance 3's different UI
- patterns.yml: WS auto-connect pattern confirmed
- Note: Instance 3 is a different app, not part of pi-agent-template
