# Session: ui-test-1777778764 (and subsequent restarts)
Date: 2026-05-03
Module: chat
URL: http://localhost:5180
Scenarios: 20 passed / 2 failed

## Tested
- A1: Page loads without error overlay — PASSED
- A2: Title bar shows "Web (WebSocket)" blue indicator — PASSED
- A3: ActivityBar has exactly 1 icon (Chat) — PASSED
- A4: Empty state shows "Start a conversation..." — PASSED (confirmed from initial load)
- A5: Bottom area has text input + Send button — PASSED
- B6-B10: Message sending, bubble alignment, bot icon, timestamps — ALL PASSED
- B11: "hello world" message appears — PASSED
- B12: 3 rapid messages all appear correctly — PASSED
- C13: Time query returns time info — PASSED
- C14: Math "12*8" returns "96" — PASSED
- C15: Help returns capability list — PASSED
- C16: "show me files" mentions file browser — FAILED (returns fallback)
- C17: "git status" mentions git — FAILED (returns generic fallback)
- C18: "xyzabc123" gets default/fallback reply — PASSED
- D19-D20: Markdown rendering (bold, lists, paragraphs) — PASSED
- E21: Mobile 375x667 layout (bottom tab bar, no ActivityBar) — PASSED
- E22: Desktop 1280x800 layout restores — PASSED
- F23: Message count badge shows correct number — PASSED
- F24: Auto-scroll to bottom — PASSED (verified via scroll position data)

## Findings
- Bot does NOT support "show me files" or "git status" — returns fallback responses
- Markdown renders properly: <strong>, <ul>/<li>, <p> tags confirmed in DOM
- User messages: bg-indigo-600, justify-end (right-aligned)
- Bot messages: bg-gray-700, justify-start (left-aligned), with bot SVG icon
- Timestamps: HH:MM format (e.g., "11:27", "11:28")
- Responsive: mobile at <768px shows bottom tab bar, hides title bar and ActivityBar
- Auto-scroll: scrollTop + clientHeight = scrollHeight (verified at bottom)
- Message counter on separate line after "Messages" text

## Issues
- agent-browser session instability: eval calls often return about:blank
- "set viewport" command breaks the page context — must re-open after
- Page needs 8-10 seconds to fully initialize (WebSocket + message history)
- Multiple agent-browser sessions from other agents can cause conflicts

## Updated
- selectors.yml: v3, added responsive, markdown, message bubble selectors
- patterns.yml: v3, added eval-based sending, responsive testing, bot capabilities
